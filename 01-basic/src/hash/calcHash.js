const calculateHash = async () => {
    // Diese Funktion wird verwendet, um asynchron einen Hash-Wert zu berechnen.

    // Schreibe hier deinen Code
};

// Warte auf die Ausführung der calculateHash-Funktion
await calculateHash();
