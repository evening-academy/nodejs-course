const list = async () => {
    // Diese Funktion wird verwendet, um asynchron eine Liste von Elementen abzurufen oder anzuzeigen.

    // Schreibe hier deinen Code
};

// Warte auf die Ausführung der list-Funktion
await list();
