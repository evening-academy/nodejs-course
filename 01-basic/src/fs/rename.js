const rename = async () => {
    // Diese Funktion wird verwendet, um asynchron eine Datei oder/und einen Ordner umzubenennen.

    // Schreibe hier deinen Code
};

// Warte auf die Ausführung der rename-Funktion
await rename();
